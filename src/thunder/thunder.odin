package thunder

// Root engine package. Real subsystems arrive with Phase 1 (data
// foundation); this package currently only anchors the build and the
// test harness (WI-0004).

VERSION :: "0.0.1"
TARGET_GAME_PATCH :: "1.29b"
